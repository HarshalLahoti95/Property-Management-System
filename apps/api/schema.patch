--- prisma/pulled.prisma	2026-07-10 11:05:41
+++ prisma/schema.prisma	2026-07-13 11:51:52
@@ -1,3 +1,6 @@
+// This is your Prisma schema file,
+// learn more about it in the docs: https://pris.ly/d/prisma-schema
+
 generator client {
   provider = "prisma-client-js"
 }
@@ -7,29 +10,216 @@
   url      = env("DATABASE_URL")
 }
 
+// ==========================================
+// ENUMS
+// ==========================================
+
+enum UserRole {
+  ADMIN
+  LANDLORD
+  TENANT
+}
+
+enum UserStatus {
+  ACTIVE
+  INACTIVE
+  SUSPENDED
+}
+
+enum PropertyType {
+  RESIDENTIAL
+  COMMERCIAL
+}
+
+enum PropertyLayout {
+  STANDALONE
+  MULTI_UNIT
+}
+
+/// RESERVED = a lease exists for this unit in any non-terminal, pre-active
+/// status (DRAFT / PENDING_LANDLORD_APPROVAL / PENDING_TENANT_SIGNATURE).
+/// Draft creation is only ever allowed when status == VACANT — that single
+/// check should be the only gate used anywhere in the app for "can a new
+/// lease be started here."
+/// NOTICE_GIVEN = the unit's current lease is in PENDING_TERMINATION_APPROVAL.
+enum UnitOccupancyStatus {
+  VACANT
+  RESERVED
+  OCCUPIED
+  NOTICE_GIVEN
+  MAINTENANCE
+}
+
+enum LeaseStatus {
+  DRAFT
+  PENDING_LANDLORD_APPROVAL
+  PENDING_TENANT_SIGNATURE
+  ACTIVE
+  PENDING_TERMINATION_APPROVAL
+  REJECTED
+  CANCELLED
+  DECLINED
+  EXPIRED
+  TERMINATED
+}
+
+enum LeaseRenewalType {
+  AUTO_MONTH_TO_MONTH
+  FIXED_END
+}
+
+/// PENDING = attached to the lease, hasn't signed yet.
+/// ACTIVE = has signed.
+/// DECLINED = refused to sign — this cascades the parent Lease to DECLINED,
+/// since a lease can only reach ACTIVE once every non-removed LeaseTenant is
+/// individually ACTIVE (see Lease business constraint comment).
+enum LeaseTenantStatus {
+  PENDING
+  ACTIVE
+  DECLINED
+  REMOVED
+}
+
+/// Distinguishes which stage of the workflow a generated document belongs to.
+/// Multiple LeaseDocument rows of different purposes can exist for one lease;
+/// use Document's built-in version chain (previousDocumentId) within a given
+/// purpose if a document is regenerated (e.g. re-rendered after an edit).
+enum LeaseDocumentPurpose {
+  DRAFT_PREVIEW
+  TENANT_SIGNATURE_COPY
+  EXECUTED
+}
+
+enum LedgerType {
+  OPERATING
+  TRUST
+}
+
+enum ChargeType {
+  RENT
+  SECURITY_DEPOSIT
+  LATE_FEE
+  UTILITY
+  MAINTENANCE
+  MISC
+}
+
+enum ChargeStatus {
+  UNPAID
+  PARTIALLY_PAID
+  PAID
+  VOIDED
+}
+
+enum PaymentMethod {
+  ACH
+  CREDIT_CARD
+  CASH
+  CHECK
+}
+
+enum PaymentStatus {
+  PENDING
+  CLEARED
+  FAILED
+  REFUNDED
+}
+
+enum DisbursementMethod {
+  MANUAL
+  STRIPE
+  RAZORPAY
+}
+
+enum DisbursementStatus {
+  PENDING
+  PAID
+  FAILED
+}
+
+enum WorkOrderPriority {
+  LOW
+  MEDIUM
+  HIGH
+  EMERGENCY
+}
+
+enum WorkOrderStatus {
+  SUBMITTED
+  ASSIGNED
+  IN_PROGRESS
+  ON_HOLD
+  RESOLVED
+  CANCELLED
+}
+
+enum DocumentCategory {
+  LEASE_AGREEMENT
+  GOVERNMENT_ID
+  INVOICE
+  RECEIPT
+  DAMAGE_PHOTO
+  OTHER
+}
+
+enum OtpStatus {
+  PENDING
+  VERIFIED
+  EXPIRED
+}
+
+enum LedgerTriggerEvent {
+  CHARGE
+  PAYMENT
+  VOID
+  REFUND
+}
+
+enum DeletionStatus {
+  PENDING_ADMIN_APPROVAL
+  PENDING_LANDLORD_APPROVAL
+  APPROVED
+  REJECTED
+}
+
+enum NotificationStatus {
+  SENT
+  FAILED
+}
+
+// ==========================================
+// AUTHENTICATION & IDENTITY
+// ==========================================
+
 model User {
-  id                    String                   @id @default(uuid())
-  email                 String                   @unique
-  fullName              String
-  phone                 String
-  role                  UserRole
-  status                UserStatus
-  createdAt             DateTime                 @default(now())
-  updatedAt             DateTime                 @updatedAt
-  deletedAt             DateTime?
-  mailingAddress        String?
-  leasesCreated         Lease[]                  @relation("LeaseCreatedBy")
-  leaseStatusHistories  LeaseStatusHistory[]     @relation("UserLeaseStatusHistory")
+  id             String     @id @default(uuid())
+  email          String     @unique
+  fullName       String
+  phone          String
+  mailingAddress String?
+  role           UserRole
+  status         UserStatus
+  createdAt      DateTime   @default(now())
+  updatedAt      DateTime   @updatedAt
+  deletedAt      DateTime?
+
+  // Relations
+  passwordCredential PasswordCredential?
+  otpRequests        OtpRequest[]
+  units              Unit[]              @relation("LandlordUnits")
+
   leaseTenants          LeaseTenant[]            @relation("TenantLeases")
-  notifications         NotificationHistory[]
-  otpRequests           OtpRequest[]
-  passwordCredential    PasswordCredential?
+  leasesCreated         Lease[]                  @relation("LeaseCreatedBy")
   payments              Payment[]                @relation("TenantPayments")
-  units                 Unit[]                   @relation("LandlordUnits")
+  disbursementsRecorded Disbursement[] @relation("DisbursementRecordedBy")
+  percentageHistoriesChanged LeaseSharePercentageHistory[] @relation("PercentageHistoryChangedBy")
+  companyDeductionsRecorded CompanyMaintenanceDeduction[] @relation("DeductionRecordedBy")
   userDocuments         UserDocument[]
-  userPreference        UserPreference?
+  leaseStatusHistories  LeaseStatusHistory[]     @relation("UserLeaseStatusHistory")
   workOrderComments     WorkOrderComment[]
   statusChangeHistories WorkOrderStatusHistory[] @relation("UserStatusHistory")
+  notifications         NotificationHistory[]
+  userPreference        UserPreference?
 
   @@index([role, status])
 }
@@ -38,7 +228,9 @@
   userId       String   @id
   passwordHash String
   updatedAt    DateTime @updatedAt
-  user         User     @relation(fields: [userId], references: [id], onDelete: Cascade)
+
+  // Relations
+  user User @relation(fields: [userId], references: [id], onDelete: Cascade)
 }
 
 model OtpRequest {
@@ -49,28 +241,38 @@
   expiresAt     DateTime
   attemptsCount Int       @default(0)
   createdAt     DateTime  @default(now())
-  user          User      @relation(fields: [userId], references: [id], onDelete: Cascade)
 
+  // Relations
+  user User @relation(fields: [userId], references: [id], onDelete: Cascade)
+
   @@index([userId, status])
 }
 
+// ==========================================
+// PHYSICAL PORTFOLIO
+// ==========================================
+
 model Property {
-  id                      String          @id @default(uuid())
-  name                    String
-  type                    PropertyType
-  layout                  PropertyLayout  @default(MULTI_UNIT)
-  streetAddress           String
-  city                    String
-  state                   String
-  zipCode                 String
-  createdAt               DateTime        @default(now())
-  updatedAt               DateTime        @updatedAt
-  deletedAt               DateTime?
-  deletionRequestedByRole UserRole?
+  id            String         @id @default(uuid())
+  name          String
+  type          PropertyType
+  layout        PropertyLayout @default(MULTI_UNIT)
+  streetAddress String
+  city          String
+  state         String
+  zipCode       String
+  createdAt     DateTime       @default(now())
+  updatedAt     DateTime       @updatedAt
+  deletedAt     DateTime?
+
   deletionStatus          DeletionStatus?
-  units                   Unit[]
-  workOrders              WorkOrder[]
+  deletionRequestedByRole UserRole?
 
+  // Relations
+  units      Unit[]
+  workOrders WorkOrder[]
+
+  // Note: Logical uniqueness check (usually filtered on active records, handled in DB partial indexes/application logic)
   @@unique([streetAddress, city, state, zipCode])
   @@index([state, city])
 }
@@ -79,32 +281,39 @@
 /// the deletionStatus approval flow while it has any Lease in a non-terminal
 /// status (i.e. occupancyStatus != VACANT).
 model Unit {
-  id                      String              @id @default(uuid())
-  propertyId              String
-  landlordId              String
-  unitNumber              String
-  floorLevel              Int
-  bedCount                Int
-  bathCount               Int
-  squareFootage           Int
-  targetRent              Decimal             @db.Decimal(18, 2)
-  occupancyStatus         UnitOccupancyStatus
-  createdAt               DateTime            @default(now())
-  updatedAt               DateTime            @updatedAt
-  deletedAt               DateTime?
-  deletionRequestedByRole UserRole?
+  id              String              @id @default(uuid())
+  propertyId      String
+  landlordId      String
+  unitNumber      String
+  floorLevel      Int
+  bedCount        Int
+  bathCount       Int
+  squareFootage   Int
+  targetRent      Decimal             @db.Decimal(18, 2)
+  occupancyStatus UnitOccupancyStatus
+  createdAt       DateTime            @default(now())
+  updatedAt       DateTime            @updatedAt
+  deletedAt       DateTime?
+
   deletionStatus          DeletionStatus?
-  leases                  Lease[]
-  landlord                User                @relation("LandlordUnits", fields: [landlordId], references: [id])
-  property                Property            @relation(fields: [propertyId], references: [id])
-  workOrders              WorkOrder[]
+  deletionRequestedByRole UserRole?
 
+  // Relations
+  property   Property    @relation(fields: [propertyId], references: [id], onDelete: Restrict)
+  landlord   User        @relation("LandlordUnits", fields: [landlordId], references: [id], onDelete: Restrict)
+  leases     Lease[]
+  workOrders WorkOrder[]
+
   @@unique([propertyId, unitNumber])
   @@index([propertyId])
   @@index([landlordId])
   @@index([occupancyStatus])
 }
 
+// ==========================================
+// AGREEMENTS & TENANCIES
+// ==========================================
+
 /// BUSINESS CONSTRAINTS:
 /// - Enforced in PostgreSQL Migration: None (Prisma/SQL standard unique indexes cannot validate date ranges).
 /// - Enforced in Application Logic: Only one ACTIVE lease per unit at any given date. Prior to activating a lease (status -> ACTIVE), application queries must verify that no other active lease overlaps with the lease's [startDate, endDate] interval.
@@ -114,28 +323,44 @@
 /// - EXPIRATION: driven by a scheduled job comparing endDate against now for all ACTIVE leases — not a passive check-on-read. If a lease is PENDING_TERMINATION_APPROVAL when its endDate arrives, expiration takes precedence and the pending termination request is closed out as moot.
 /// - RENEWAL: renewalType == AUTO_MONTH_TO_MONTH changes what should happen at endDate — this business logic is not yet implemented and needs an explicit decision before the expiration job is built.
 model Lease {
-  id                   String               @id @default(uuid())
-  unitId               String
-  startDate            DateTime
-  endDate              DateTime
-  monthlyRent          Decimal              @db.Decimal(18, 2)
-  securityDeposit      Decimal              @db.Decimal(18, 2)
-  rentDueDay           Int
-  gracePeriodDays      Int
-  status               LeaseStatus
-  createdAt            DateTime             @default(now())
-  updatedAt            DateTime             @updatedAt
-  deletedAt            DateTime?
-  createdByRole        UserRole             @default(LANDLORD)
-  createdByUserId      String?
-  leaseTermsJson       Json?
-  renewalType          LeaseRenewalType     @default(FIXED_END)
-  financialLedgers     FinancialLedger[]
+  id              String           @id @default(uuid())
+  unitId          String
+  // Note: This is required for all new leases created via the new service layer,
+  // but existing rows may have null values. Enforced at the application level in leaseStatusService.
+  createdByUserId String?
+  createdByRole   UserRole         @default(LANDLORD)
+  startDate       DateTime
+  endDate         DateTime
+  monthlyRent     Decimal          @db.Decimal(18, 2)
+  securityDeposit Decimal          @db.Decimal(18, 2)
+  rentDueDay      Int
+  gracePeriodDays Int
+  renewalType     LeaseRenewalType @default(FIXED_END)
+  // Narrative/legal fields that are rendered into the generated document but
+  // never queried or filtered on directly: included items, payment methods,
+  // utilities split, occupants/pets, subletting, maintenance responsibilities,
+  // entry notice, termination notice, early termination fee, house rules,
+  // additional terms. Shape matches the field schema used to drive the form.
+  leaseTermsJson  Json?
+  status          LeaseStatus
+  createdAt       DateTime         @default(now())
+  updatedAt       DateTime         @updatedAt
+  deletedAt       DateTime?
+
+  // Relations
+  unit                 Unit                 @relation(fields: [unitId], references: [id], onDelete: Restrict)
   createdByUser        User?                @relation("LeaseCreatedBy", fields: [createdByUserId], references: [id], onDelete: Restrict)
-  unit                 Unit                 @relation(fields: [unitId], references: [id])
+  leaseTenants         LeaseTenant[]
+  financialLedgers     FinancialLedger[]
   leaseDocuments       LeaseDocument[]
   leaseStatusHistories LeaseStatusHistory[] @relation("LeaseStatusHistoryTrack")
-  leaseTenants         LeaseTenant[]
+  rentCharges          RentCharge[]
+  payments             Payment[]
+  sharePercentageHistories LeaseSharePercentageHistory[]
+  chargeSplitRules     ChargeSplitRule[]
+  disbursements        Disbursement[]
+  companyMaintenanceDeductions CompanyMaintenanceDeduction[]
+  accountingConfig     LeaseAccountingConfig?
 
   @@index([unitId])
   @@index([status])
@@ -148,106 +373,192 @@
   leaseId    String
   tenantId   String
   status     LeaseTenantStatus @default(PENDING)
+  signedAt   DateTime?
+  declinedAt DateTime?
   joinedAt   DateTime          @default(now())
   createdAt  DateTime          @default(now())
   updatedAt  DateTime          @updatedAt
-  declinedAt DateTime?
-  signedAt   DateTime?
-  lease      Lease             @relation(fields: [leaseId], references: [id])
-  tenant     User              @relation("TenantLeases", fields: [tenantId], references: [id])
 
+  // Relations
+  lease  Lease @relation(fields: [leaseId], references: [id], onDelete: Restrict)
+  tenant User  @relation("TenantLeases", fields: [tenantId], references: [id], onDelete: Restrict)
+
   @@unique([leaseId, tenantId])
   @@index([tenantId])
 }
 
-/// BUSINESS CONSTRAINT:
-/// - Enforced in PostgreSQL Migration: Composite Unique Constraint @@unique([leaseId, ledgerType]) which prevents duplicate ledgers of the same class for a lease.
-/// - Enforced in Application Logic: Ledger balance invariants. The runningBalance must always equal (Sum of Charges) - (Sum of Payments) for the ledger. Nightly async checks validate this invariant and flag anomalies.
-/// - Not modified as part of the lease-workflow update. Flagging one touchpoint: Operating + Trust ledger rows need to be created somewhere the moment a Lease transitions to ACTIVE, if that isn't already owned elsewhere.
+// ==========================================
+// DOUBLE ENTRY TRUST LEDGER SYSTEM (REBUILT)
+// ==========================================
+
 model FinancialLedger {
-  id                     String                 @id @default(uuid())
-  leaseId                String
-  ledgerType             LedgerType
-  runningBalance         Decimal                @default(0.00) @db.Decimal(18, 2)
-  createdAt              DateTime               @default(now())
-  updatedAt              DateTime               @updatedAt
-  lease                  Lease                  @relation(fields: [leaseId], references: [id])
-  ledgerBalanceHistories LedgerBalanceHistory[]
-  payments               Payment[]
-  rentCharges            RentCharge[]
+  id             String     @id @default(uuid())
+  leaseId        String
+  ledgerType     LedgerType
+  runningBalance Decimal    @default(0.00) @db.Decimal(18, 2)
+  createdAt      DateTime   @default(now())
+  updatedAt      DateTime   @updatedAt
 
+  lease Lease @relation(fields: [leaseId], references: [id], onDelete: Restrict)
+
   @@unique([leaseId, ledgerType])
   @@index([leaseId])
 }
 
 model RentCharge {
-  id                 String              @id @default(uuid())
-  ledgerId           String
-  dueDate            DateTime
-  type               ChargeType
-  amount             Decimal             @db.Decimal(18, 2)
-  paidAmount         Decimal             @default(0.00) @db.Decimal(18, 2)
-  status             ChargeStatus
-  description        String
-  createdAt          DateTime            @default(now())
-  updatedAt          DateTime            @updatedAt
+  id           String       @id @default(uuid())
+  leaseId      String
+  billingMonth DateTime
+  type         ChargeType
+  amount       Decimal      @db.Decimal(18, 2)
+  paidAmount   Decimal      @default(0.00) @db.Decimal(18, 2)
+  status       ChargeStatus
+  dueDate      DateTime
+  description  String
+  createdAt    DateTime     @default(now())
+  updatedAt    DateTime     @updatedAt
+
+  lease              Lease               @relation(fields: [leaseId], references: [id], onDelete: Restrict)
   paymentAllocations PaymentAllocation[]
-  ledger             FinancialLedger     @relation(fields: [ledgerId], references: [id])
 
-  @@index([ledgerId])
-  @@index([ledgerId, status])
+  @@index([leaseId, status])
+  @@index([billingMonth])
   @@index([dueDate])
 }
 
 model Payment {
-  id                   String              @id @default(uuid())
-  ledgerId             String
+  id                   String        @id @default(uuid())
+  leaseId              String
   tenantId             String
-  amount               Decimal             @db.Decimal(18, 2)
+  billingMonth         DateTime
+  amount               Decimal       @db.Decimal(18, 2)
   paymentMethod        PaymentMethod
-  transactionReference String              @unique
+  transactionReference String        @unique
   status               PaymentStatus
   paymentDate          DateTime
-  createdAt            DateTime            @default(now())
-  updatedAt            DateTime            @updatedAt
-  ledger               FinancialLedger     @relation(fields: [ledgerId], references: [id])
-  tenant               User                @relation("TenantPayments", fields: [tenantId], references: [id])
-  paymentAllocations   PaymentAllocation[]
-  paymentDocuments     PaymentDocument[]
+  createdAt            DateTime      @default(now())
+  updatedAt            DateTime      @updatedAt
 
-  @@index([ledgerId])
+  lease              Lease               @relation(fields: [leaseId], references: [id], onDelete: Restrict)
+  tenant             User                @relation("TenantPayments", fields: [tenantId], references: [id], onDelete: Restrict)
+  paymentAllocations PaymentAllocation[]
+
+  @@unique([leaseId, billingMonth])
+  @@index([leaseId])
   @@index([tenantId])
 }
 
-/// BUSINESS CONSTRAINT:
-/// - Enforced in PostgreSQL Migration: Composite Unique Constraint @@unique([paymentId, rentChargeId]) to prevent duplicate mappings.
-/// - Enforced in Application Logic: Chronological payment allocation (FIFO). Payments must clear ledger charges chronologically (oldest unpaid charges first) or by type priority (e.g. security deposit, rent, fees).
 model PaymentAllocation {
-  id              String     @id @default(uuid())
-  paymentId       String
-  rentChargeId    String
-  amountAllocated Decimal    @db.Decimal(18, 2)
-  allocatedAt     DateTime   @default(now())
-  createdAt       DateTime   @default(now())
-  updatedAt       DateTime   @updatedAt
-  payment         Payment    @relation(fields: [paymentId], references: [id])
-  rentCharge      RentCharge @relation(fields: [rentChargeId], references: [id])
+  id                  String   @id @default(uuid())
+  paymentId           String
+  rentChargeId        String
+  landlordShareAmount Decimal  @db.Decimal(18, 2)
+  companyShareAmount  Decimal  @db.Decimal(18, 2)
+  allocatedAt         DateTime @default(now())
+  createdAt           DateTime @default(now())
+  updatedAt           DateTime @updatedAt
 
+  payment    Payment    @relation(fields: [paymentId], references: [id], onDelete: Restrict)
+  rentCharge RentCharge @relation(fields: [rentChargeId], references: [id], onDelete: Restrict)
+
   @@unique([paymentId, rentChargeId])
   @@index([paymentId])
   @@index([rentChargeId])
 }
 
+model LeaseSharePercentageHistory {
+  id                      String   @id @default(uuid())
+  leaseId                 String
+  landlordSharePercentage Decimal  @db.Decimal(5, 2)
+  effectiveFrom           DateTime
+  changedByUserId         String
+  createdAt               DateTime @default(now())
+
+  lease         Lease @relation(fields: [leaseId], references: [id], onDelete: Restrict)
+  changedByUser User  @relation("PercentageHistoryChangedBy", fields: [changedByUserId], references: [id], onDelete: Restrict)
+
+  @@index([leaseId])
+  @@index([changedByUserId])
+  @@index([effectiveFrom])
+}
+
+model ChargeSplitRule {
+  id                      String     @id @default(uuid())
+  leaseId                 String
+  chargeType              ChargeType
+  landlordSharePercentage Decimal    @db.Decimal(5, 2)
+  createdAt               DateTime   @default(now())
+  updatedAt               DateTime   @updatedAt
+
+  lease Lease @relation(fields: [leaseId], references: [id], onDelete: Restrict)
+
+  @@unique([leaseId, chargeType])
+  @@index([leaseId])
+}
+
+model Disbursement {
+  id               String             @id @default(uuid())
+  leaseId          String
+  amount           Decimal            @db.Decimal(18, 2)
+  method           DisbursementMethod
+  status           DisbursementStatus
+  reference        String?
+  recordedByUserId String
+  createdAt        DateTime           @default(now())
+  updatedAt        DateTime           @updatedAt
+
+  lease          Lease @relation(fields: [leaseId], references: [id], onDelete: Restrict)
+  recordedByUser User  @relation("DisbursementRecordedBy", fields: [recordedByUserId], references: [id], onDelete: Restrict)
+
+  @@index([leaseId])
+  @@index([recordedByUserId])
+}
+
+model CompanyMaintenanceDeduction {
+  id               String   @id @default(uuid())
+  leaseId          String
+  workOrderId      String
+  amount           Decimal  @db.Decimal(18, 2)
+  deductionMonth   DateTime
+  recordedByUserId String
+  createdAt        DateTime @default(now())
+
+  lease          Lease     @relation(fields: [leaseId], references: [id], onDelete: Restrict)
+  workOrder      WorkOrder @relation(fields: [workOrderId], references: [id], onDelete: Restrict)
+  recordedByUser User      @relation("DeductionRecordedBy", fields: [recordedByUserId], references: [id], onDelete: Restrict)
+
+  @@index([leaseId])
+  @@index([workOrderId])
+  @@index([recordedByUserId])
+}
+
+model LeaseAccountingConfig {
+  id                     String   @id @default(uuid())
+  leaseId                String   @unique
+  tenantGracePeriodDays  Int      @default(0)
+  companyGracePeriodDays Int      @default(0)
+  createdAt              DateTime @default(now())
+  updatedAt              DateTime @updatedAt
+
+  lease Lease @relation(fields: [leaseId], references: [id], onDelete: Restrict)
+}
+
+// ==========================================
+// MAINTENANCE & VENDORS
+// ==========================================
+
 model Vendor {
-  id                String                   @id @default(uuid())
-  companyName       String
-  contactName       String
-  serviceCategory   String
-  email             String                   @unique
-  phone             String
-  createdAt         DateTime                 @default(now())
-  updatedAt         DateTime                 @updatedAt
-  deletedAt         DateTime?
+  id              String    @id @default(uuid())
+  companyName     String
+  contactName     String
+  serviceCategory String
+  email           String    @unique
+  phone           String
+  createdAt       DateTime  @default(now())
+  updatedAt       DateTime  @updatedAt
+  deletedAt       DateTime?
+
+  // Relations
   workOrders        WorkOrder[]
   assignedHistories WorkOrderStatusHistory[] @relation("AssignedVendorHistory")
   previousHistories WorkOrderStatusHistory[] @relation("PreviousVendorHistory")
@@ -256,54 +567,62 @@
 }
 
 model WorkOrder {
-  id                   String                   @id @default(uuid())
+  id                   String            @id @default(uuid())
   propertyId           String
   unitId               String?
   vendorId             String?
-  workOrderNumber      String                   @unique
+  workOrderNumber      String            @unique
   title                String
   description          String
   priority             WorkOrderPriority
   status               WorkOrderStatus
-  estimatedCost        Decimal?                 @db.Decimal(18, 2)
-  actualCost           Decimal?                 @db.Decimal(18, 2)
+  estimatedCost        Decimal?          @db.Decimal(18, 2)
+  actualCost           Decimal?          @db.Decimal(18, 2)
   targetCompletionDate DateTime?
   actualCompletionDate DateTime?
-  createdAt            DateTime                 @default(now())
-  updatedAt            DateTime                 @updatedAt
+  createdAt            DateTime          @default(now())
+  updatedAt            DateTime          @updatedAt
   deletedAt            DateTime?
-  property             Property                 @relation(fields: [propertyId], references: [id])
-  unit                 Unit?                    @relation(fields: [unitId], references: [id], onDelete: Restrict)
-  vendor               Vendor?                  @relation(fields: [vendorId], references: [id], onDelete: Restrict)
-  comments             WorkOrderComment[]
-  workOrderDocuments   WorkOrderDocument[]
-  statusHistory        WorkOrderStatusHistory[]
 
+  // Relations
+  property           Property                 @relation(fields: [propertyId], references: [id], onDelete: Restrict)
+  unit               Unit?                    @relation(fields: [unitId], references: [id], onDelete: Restrict)
+  vendor             Vendor?                  @relation(fields: [vendorId], references: [id], onDelete: Restrict)
+  workOrderDocuments WorkOrderDocument[]
+  comments           WorkOrderComment[]
+  statusHistory      WorkOrderStatusHistory[]
+  companyMaintenanceDeductions CompanyMaintenanceDeduction[]
+
   @@index([propertyId])
   @@index([unitId])
   @@index([vendorId])
   @@index([status])
 }
 
+// ==========================================
+// DOCUMENT STORAGE & ATTACHMENTS
+// ==========================================
+
 model Document {
-  id                 String              @id @default(uuid())
+  id                 String           @id @default(uuid())
   fileName           String
   fileSize           Int
   fileType           String
   storageUrl         String
-  hash               String              @unique
+  hash               String           @unique
   category           DocumentCategory
-  createdAt          DateTime            @default(now())
-  updatedAt          DateTime            @updatedAt
+  createdAt          DateTime         @default(now())
+  updatedAt          DateTime         @updatedAt
   deletedAt          DateTime?
-  previousDocumentId String?             @unique
-  previousDocument   Document?           @relation("DocumentVersionChain", fields: [previousDocumentId], references: [id])
+  previousDocumentId String?          @unique
+
+  // Relations
+  previousDocument   Document?           @relation("DocumentVersionChain", fields: [previousDocumentId], references: [id], onDelete: SetNull)
   nextDocument       Document?           @relation("DocumentVersionChain")
   leaseDocuments     LeaseDocument[]
-  paymentDocuments   PaymentDocument[]
   userDocuments      UserDocument[]
   workOrderDocuments WorkOrderDocument[]
-}
+  }
 
 /// A lease can have multiple LeaseDocument rows of different purposes at once
 /// (e.g. a DRAFT_PREVIEW while under landlord review, later a
@@ -315,12 +634,14 @@
   id         String               @id @default(uuid())
   leaseId    String
   documentId String
+  purpose    LeaseDocumentPurpose
   createdAt  DateTime             @default(now())
   updatedAt  DateTime             @updatedAt
-  purpose    LeaseDocumentPurpose
-  document   Document             @relation(fields: [documentId], references: [id], onDelete: Cascade)
-  lease      Lease                @relation(fields: [leaseId], references: [id], onDelete: Cascade)
 
+  // Relations
+  lease    Lease    @relation(fields: [leaseId], references: [id], onDelete: Cascade)
+  document Document @relation(fields: [documentId], references: [id], onDelete: Cascade)
+
   @@unique([leaseId, documentId])
   @@index([leaseId])
   @@index([documentId])
@@ -333,41 +654,36 @@
   documentId String
   createdAt  DateTime @default(now())
   updatedAt  DateTime @updatedAt
-  document   Document @relation(fields: [documentId], references: [id], onDelete: Cascade)
-  user       User     @relation(fields: [userId], references: [id], onDelete: Cascade)
 
+  // Relations
+  user     User     @relation(fields: [userId], references: [id], onDelete: Cascade)
+  document Document @relation(fields: [documentId], references: [id], onDelete: Cascade)
+
   @@unique([userId, documentId])
   @@index([userId])
   @@index([documentId])
 }
 
 model WorkOrderDocument {
-  id          String    @id @default(uuid())
+  id          String   @id @default(uuid())
   workOrderId String
   documentId  String
-  createdAt   DateTime  @default(now())
-  updatedAt   DateTime  @updatedAt
-  document    Document  @relation(fields: [documentId], references: [id], onDelete: Cascade)
-  workOrder   WorkOrder @relation(fields: [workOrderId], references: [id], onDelete: Cascade)
+  createdAt   DateTime @default(now())
+  updatedAt   DateTime @updatedAt
 
+  // Relations
+  workOrder WorkOrder @relation(fields: [workOrderId], references: [id], onDelete: Cascade)
+  document  Document  @relation(fields: [documentId], references: [id], onDelete: Cascade)
+
   @@unique([workOrderId, documentId])
   @@index([workOrderId])
   @@index([documentId])
 }
 
-model PaymentDocument {
-  id         String   @id @default(uuid())
-  paymentId  String
-  documentId String
-  createdAt  DateTime @default(now())
-  updatedAt  DateTime @updatedAt
-  document   Document @relation(fields: [documentId], references: [id], onDelete: Cascade)
-  payment    Payment  @relation(fields: [paymentId], references: [id], onDelete: Cascade)
 
-  @@unique([paymentId, documentId])
-  @@index([paymentId])
-  @@index([documentId])
-}
+// ==========================================
+// SYSTEM AUDIT MODULES
+// ==========================================
 
 /// oldStatus is nullable so the very first row for a lease (creation itself)
 /// can be logged as null -> DRAFT, changedByUserId = the creator. Every
@@ -381,35 +697,32 @@
   changedByUserId   String
   reasonDescription String?
   changedAt         DateTime     @default(now())
-  changedByUser     User         @relation("UserLeaseStatusHistory", fields: [changedByUserId], references: [id])
-  lease             Lease        @relation("LeaseStatusHistoryTrack", fields: [leaseId], references: [id])
 
+  // Relations
+  lease         Lease @relation("LeaseStatusHistoryTrack", fields: [leaseId], references: [id], onDelete: Restrict)
+  changedByUser User  @relation("UserLeaseStatusHistory", fields: [changedByUserId], references: [id], onDelete: Restrict)
+
   @@index([leaseId])
   @@index([changedByUserId])
 }
 
-model LedgerBalanceHistory {
-  id               String             @id @default(uuid())
-  ledgerId         String
-  oldBalance       Decimal            @db.Decimal(18, 2)
-  newBalance       Decimal            @db.Decimal(18, 2)
-  triggerEventType LedgerTriggerEvent
-  triggerEventId   String
-  changedAt        DateTime           @default(now())
-  ledger           FinancialLedger    @relation(fields: [ledgerId], references: [id])
 
-  @@index([ledgerId])
-}
 
+// ==========================================
+// MAINTENANCE AUDIT & DISCUSSION
+// ==========================================
+
 model WorkOrderComment {
-  id          String    @id @default(uuid())
+  id          String   @id @default(uuid())
   workOrderId String
   authorId    String
   body        String
-  createdAt   DateTime  @default(now())
-  author      User      @relation(fields: [authorId], references: [id])
-  workOrder   WorkOrder @relation(fields: [workOrderId], references: [id], onDelete: Cascade)
+  createdAt   DateTime @default(now())
 
+  // Relations
+  workOrder WorkOrder @relation(fields: [workOrderId], references: [id], onDelete: Cascade)
+  author    User      @relation(fields: [authorId], references: [id], onDelete: Restrict)
+
   @@index([workOrderId])
   @@index([authorId])
 }
@@ -424,17 +737,23 @@
   previousVendorId  String?
   reasonDescription String?
   changedAt         DateTime         @default(now())
-  assignedVendor    Vendor?          @relation("AssignedVendorHistory", fields: [assignedVendorId], references: [id])
-  changedByUser     User             @relation("UserStatusHistory", fields: [changedByUserId], references: [id])
-  previousVendor    Vendor?          @relation("PreviousVendorHistory", fields: [previousVendorId], references: [id])
-  workOrder         WorkOrder        @relation(fields: [workOrderId], references: [id], onDelete: Cascade)
 
+  // Relations
+  workOrder      WorkOrder @relation(fields: [workOrderId], references: [id], onDelete: Cascade)
+  changedByUser  User      @relation("UserStatusHistory", fields: [changedByUserId], references: [id], onDelete: Restrict)
+  assignedVendor Vendor?   @relation("AssignedVendorHistory", fields: [assignedVendorId], references: [id], onDelete: SetNull)
+  previousVendor Vendor?   @relation("PreviousVendorHistory", fields: [previousVendorId], references: [id], onDelete: SetNull)
+
   @@index([workOrderId])
   @@index([changedByUserId])
   @@index([assignedVendorId])
   @@index([previousVendorId])
 }
 
+// ==========================================
+// NOTIFICATION HISTORY & PREFERENCES
+// ==========================================
+
 model NotificationHistory {
   id             String             @id @default(uuid())
   userId         String?
@@ -446,8 +765,10 @@
   retryCount     Int                @default(0)
   deliveryResult String?
   createdAt      DateTime           @default(now())
-  user           User?              @relation(fields: [userId], references: [id], onDelete: Cascade)
 
+  // Relations
+  user User? @relation(fields: [userId], references: [id], onDelete: Cascade)
+
   @@index([userId])
   @@index([status])
 }
@@ -461,168 +782,7 @@
   marketingEmailsEnabled Boolean  @default(true)
   createdAt              DateTime @default(now())
   updatedAt              DateTime @updatedAt
-  user                   User     @relation(fields: [userId], references: [id], onDelete: Cascade)
-}
 
-enum UserRole {
-  ADMIN
-  LANDLORD
-  TENANT
+  // Relations
+  user User @relation(fields: [userId], references: [id], onDelete: Cascade)
 }
-
-enum UserStatus {
-  ACTIVE
-  INACTIVE
-  SUSPENDED
-}
-
-enum PropertyType {
-  RESIDENTIAL
-  COMMERCIAL
-}
-
-enum PropertyLayout {
-  STANDALONE
-  MULTI_UNIT
-}
-
-/// RESERVED = a lease exists for this unit in any non-terminal, pre-active
-/// status (DRAFT / PENDING_LANDLORD_APPROVAL / PENDING_TENANT_SIGNATURE).
-/// Draft creation is only ever allowed when status == VACANT — that single
-/// check should be the only gate used anywhere in the app for "can a new
-/// lease be started here."
-/// NOTICE_GIVEN = the unit's current lease is in PENDING_TERMINATION_APPROVAL.
-enum UnitOccupancyStatus {
-  VACANT
-  OCCUPIED
-  NOTICE_GIVEN
-  MAINTENANCE
-  RESERVED
-}
-
-enum LeaseStatus {
-  DRAFT
-  PENDING_LANDLORD_APPROVAL
-  PENDING_TENANT_SIGNATURE
-  ACTIVE
-  PENDING_TERMINATION_APPROVAL
-  EXPIRED
-  TERMINATED
-  REJECTED
-  CANCELLED
-  DECLINED
-}
-
-enum LeaseRenewalType {
-  AUTO_MONTH_TO_MONTH
-  FIXED_END
-}
-
-/// PENDING = attached to the lease, hasn't signed yet.
-/// ACTIVE = has signed.
-/// DECLINED = refused to sign — this cascades the parent Lease to DECLINED,
-/// since a lease can only reach ACTIVE once every non-removed LeaseTenant is
-/// individually ACTIVE (see Lease business constraint comment).
-enum LeaseTenantStatus {
-  ACTIVE
-  REMOVED
-  PENDING
-  DECLINED
-}
-
-/// Distinguishes which stage of the workflow a generated document belongs to.
-/// Multiple LeaseDocument rows of different purposes can exist for one lease;
-/// use Document's built-in version chain (previousDocumentId) within a given
-/// purpose if a document is regenerated (e.g. re-rendered after an edit).
-enum LeaseDocumentPurpose {
-  DRAFT_PREVIEW
-  TENANT_SIGNATURE_COPY
-  EXECUTED
-}
-
-enum LedgerType {
-  OPERATING
-  TRUST
-}
-
-enum ChargeType {
-  RENT
-  SECURITY_DEPOSIT
-  LATE_FEE
-  UTILITY
-  MAINTENANCE
-  MISC
-}
-
-enum ChargeStatus {
-  UNPAID
-  PARTIALLY_PAID
-  PAID
-  VOIDED
-}
-
-enum PaymentMethod {
-  ACH
-  CREDIT_CARD
-  CASH
-  CHECK
-}
-
-enum PaymentStatus {
-  PENDING
-  CLEARED
-  FAILED
-  REFUNDED
-}
-
-enum WorkOrderPriority {
-  LOW
-  MEDIUM
-  HIGH
-  EMERGENCY
-}
-
-enum WorkOrderStatus {
-  SUBMITTED
-  ASSIGNED
-  IN_PROGRESS
-  ON_HOLD
-  RESOLVED
-  CANCELLED
-}
-
-enum DocumentCategory {
-  LEASE_AGREEMENT
-  GOVERNMENT_ID
-  INVOICE
-  RECEIPT
-  DAMAGE_PHOTO
-  OTHER
-}
-
-enum OtpStatus {
-  PENDING
-  VERIFIED
-  EXPIRED
-}
-
-enum LedgerTriggerEvent {
-  CHARGE
-  PAYMENT
-  VOID
-  REFUND
-}
-
-enum DeletionStatus {
-  PENDING_ADMIN_APPROVAL
-  PENDING_LANDLORD_APPROVAL
-  APPROVED
-  REJECTED
-}
-
-enum NotificationStatus {
-  SENT
-  FAILED
-}
-
-
